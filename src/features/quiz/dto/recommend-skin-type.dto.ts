export interface RecommendSkintypeDto {
  customersurveyId: number; // ID của Customer Survey cần lấy dữ liệu
}

export interface RecommendSkintypeResponse {
  skintypeDescription: string; // Mô tả loại da trả về từ API
}
